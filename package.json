{
  "name": "cable.js",
  "version": "0.0.3",
  "description": "everything needed to sign and generate all posts and message types of the cable wire protocol",
  "main": "index.js",
  "scripts": {
    "test": "$(npm bin)/tape test/*.js | $(npm bin)/tap-spec",
    "bail": "$(npm bin)/tape test/*.js | $(npm bin)/tap-bail | $(npm bin)/tap-spec "
  },
  "author": "cabal-club authors",
  "license": "LGPL-3.0-or-later",
  "dependencies": {
    "b4a": "1.6.4",
    "sodium-universal": "3.1.0",
    "varint": "6.0.0"
  },
  "devDependencies": {
    "tap-bail": "1.0.0",
    "tap-spec": "5.0.0",
    "tape": "5.6.3"
  }
}
